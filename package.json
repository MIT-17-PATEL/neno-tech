{
  "name": "neno-tech-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Neno Technology Enterprise Website Workspace",
  "scripts": {
    "dev": "npm --prefix source run dev",
    "build": "npm --prefix source run build",
    "start": "npm --prefix source start",
    "lint": "npm --prefix source run lint"
  }
}
