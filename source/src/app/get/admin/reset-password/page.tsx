import { ResetPassword } from '@/components/admin/ResetPassword';

export const metadata = { title: 'Neno Admin | Reset Password' };

export default function ResetPasswordPage() {
  return <ResetPassword />;
}
